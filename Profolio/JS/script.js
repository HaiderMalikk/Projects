// Add any JavaScript functionality if needed
// For example, handling click events on project squares
document.querySelectorAll('.project-square').forEach(square => {
    square.addEventListener('click', () => {
        // Handle click event for project square
        // You can redirect to the project page or perform any other action
    });
});
