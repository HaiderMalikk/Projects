# to use your own images use any tool like MS Paint or ETC to draw a 28x28 pixel image make sure the drawing is black while the BG is white 
the model is already trained but you can train it on different data and make it even better
